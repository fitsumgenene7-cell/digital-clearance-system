<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<VBox alignment="CENTER" spacing="40"
      style="-fx-background-color: linear-gradient(to bottom, #3498db, #2c3e50); -fx-padding: 50;"
      xmlns="http://javafx.com/javafx/17"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="org.example.HeroPageController">

    <!-- Title -->
    <VBox alignment="CENTER" spacing="10">
        <Label text="🎓" style="-fx-font-size: 80px;"/>
        <Label text="Digital Clearance System"
               style="-fx-font-size: 32px; -fx-font-weight: bold; -fx-text-fill: white;"/>
    </VBox>

    <!-- Login Options -->
    <HBox alignment="CENTER" spacing="40">
        <!-- Student Login Card -->
        <VBox alignment="CENTER" spacing="20"
              style="-fx-background-color: white; -fx-background-radius: 15; -fx-padding: 30; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.2), 10, 0, 0, 5);">
            <Label text="👨‍🎓" style="-fx-font-size: 40px;"/>
            <Label text="Student Login"
                   style="-fx-font-size: 20px; -fx-font-weight: bold;"/>
            <Button text="Login as Student"
                    onAction="#goToStudentLogin"
                    style="-fx-background-color: #3498db; -fx-text-fill: white; -fx-font-weight: bold; -fx-padding: 10 30;"/>
        </VBox>

        <!-- Office Login Card -->
        <VBox alignment="CENTER" spacing="20"
              style="-fx-background-color: white; -fx-background-radius: 15; -fx-padding: 30; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.2), 10, 0, 0, 5);">
            <Label text="🏢" style="-fx-font-size: 40px;"/>
            <Label text="Office Login"
                   style="-fx-font-size: 20px; -fx-font-weight: bold;"/>
            <Button text="Login as Office"
                    onAction="#goToOfficeLogin"
                    style="-fx-background-color: #e74c3c; -fx-text-fill: white; -fx-font-weight: bold; -fx-padding: 10 30;"/>
        </VBox>
    </HBox>

    <!-- Footer -->
    <Label text="Select your login method to continue"
           style="-fx-text-fill: #bdc3c7; -fx-font-size: 14px;"/>
</VBox>